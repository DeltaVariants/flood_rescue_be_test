import request from 'supertest';
import app from '../../../src/app.js';
// import { connectTestDB, clearTestDB, closeTestDB } from '../setup/db.setup.js';

describe('Mission Control - Integration Tests (Duy)', () => {
    // beforeAll(async () => await connectTestDB());
    // afterAll(async () => await closeTestDB());
    
    it('should create a DRAFT mission successfully as a Coordinator', async () => {
        // TODO: MOCK TOKEN VÀ SUPERTEST Ở ĐÂY
    });
});
